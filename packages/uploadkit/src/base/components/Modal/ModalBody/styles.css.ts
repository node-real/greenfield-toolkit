import { style } from '@vanilla-extract/css';

export const clsModalBody = style({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  textAlign: 'center',
  overflow: 'auto',
  flex: 1,
});
