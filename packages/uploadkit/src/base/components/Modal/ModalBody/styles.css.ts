import { style } from '@vanilla-extract/css';

export const clsModalBody = style({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  textAlign: 'center',
  overflow: 'scroll',
  flex: 1,
});
