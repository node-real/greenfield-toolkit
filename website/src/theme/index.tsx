import { Theme } from '@totejs/uikit';

export const theme: Theme = {
  config: {
    useSystemColorMode: false,
    initialColorMode: 'system',
    storageType: 'localStorage',
    storageKey: 'uploadkit-color-mode',
  },
};
